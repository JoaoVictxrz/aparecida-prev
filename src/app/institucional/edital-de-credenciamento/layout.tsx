export function metadata() {
  return {
    title: "Edital de Credenciamento | AparecidaPrev",
  };
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

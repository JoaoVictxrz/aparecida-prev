export const metadata = {
  title: "CONCURSO PÚBLICO APARECIDAPREV N°001/2017 | Aparecida Prev",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
